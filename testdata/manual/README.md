# Manual offline fixtures

Put real, local-only offline test files in this directory:

- `sample.lvx2`
- `sample.pcap`

These files are intentionally ignored by Git because they can be large and may contain captured device data. They are used for manual smoke checks after each refactor stage.
